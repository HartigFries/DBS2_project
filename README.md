After Running the python scripts that have corresponding csvs, we run a script having the suffix `_regen` to fix any mistakes.

For MongoDB, we have a folder with the json format that we would need to insert the data we require. 